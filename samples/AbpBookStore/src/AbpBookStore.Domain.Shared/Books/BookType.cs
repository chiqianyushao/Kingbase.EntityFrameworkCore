namespace AbpBookStore.Books;

/// <summary>
/// Mirrors the BookType enum from the ABP BookStore tutorial
/// (Acme.BookStore.Domain.Shared).
/// </summary>
public enum BookType
{
    Undefined,
    Adventure,
    Biography,
    Dystopia,
    Fantastic,
    Horror,
    Science,
    ScienceFiction,
    Poetry
}
