# EF Core 10 - KingbaseES Provider 全语法兼容性分析报告

- 报告日期：2026-08-04
- EF Core 基线：10.0.9
- Provider：Kingbase.EntityFrameworkCore 10.0.0-alpha.3
- ADO.NET 驱动：Kdbndp_V9 10.0.1.703（net10.0）
- 实测数据库：KingbaseES V009R001C002B0014，`database_mode=oracle`
- 隔离测试库：`efcore10_kingbase_dev`

> **最终结论：当前仍不能宣称 EF Core 10 所有语法均支持，但本轮已清空现有单节点环境中可补齐的主要“未验证”项。** 当前版本通过 58 项 Release 回归，其中 34 项为真实 Kdbndp 集成测试；新增验证关系型 complex type、空/大参数集合、微秒/纳秒/DayNumber、null 补偿、Enum、内联 byte[]、JSON 文档、int[]/List<int>、range、全文函数、DataSource/单主机 MultiHost、通知跟踪、Seeding、Interceptor、Index INCLUDE 和 NativeAOT。剩余缺口均在下文标明具体限制。

## 1. 状态定义

| 标记 | 含义 |
|---|---|
| ✅ 已验证 | 已在真实 KingbaseES + Kdbndp 上执行并通过，或纯 EF Core 客户端行为与数据库无关且已有测试 |
| 🟡 基础支持 | EF Core 关系型默认实现能够处理，当前 Provider 没有专用实现；只适合简单场景，未完成完整规范测试 |
| 🟠 部分支持 | 一部分语法可用，但复杂形态、数据库语义或边界条件不完整 |
| ❌ 不支持 | 当前缺少类型映射、SQL 翻译、服务注册或数据库实现，调用会失败或不能保证语义 |
| ⛔ EF 无服务端语义 | EF Core 10 关系型查询管线没有该 `Queryable` 操作的服务端翻译入口；不能由 Kingbase SQL 方言层单独补齐，若强制支持需要修改/分叉 EF Core 查询核心 |
| 🚫 不适用 | 其他数据库 Provider 的专有功能，不属于 KingbaseES 公共 EF Core 兼容范围 |

“EF 无服务端语义”并不是说该 LINQ 方法在 .NET 中不能使用，而是说：方法只能在 `IEnumerable`/`AsEnumerable()` 之后由 .NET 客户端执行，EF Core 10 不会把它转换为数据库 SQL。例如任意 accumulator 的 `Aggregate`、按固定大小返回嵌套集合的 `Chunk`、依赖两个有序序列逐项配对的 `Zip`，都没有通用且语义等价的关系型 SQL 表达。数据库 Provider 只能实现 EF Core 已交给 Provider 的 SQL 方言部分；若要让这些方法保持原始 `IQueryable` 用法并在服务器执行，需要修改 EF Core 查询核心、定义排序/空值/重复值语义并新增 SQL 树节点，不能通过增加一个 Kingbase 函数翻译器完成。

## 2. 审计依据

1. EF Core 10.0.9 官方程序集反射：`Queryable`、`EntityFrameworkQueryableExtensions`、`RelationalQueryableExtensions`、`RelationalDatabaseFacadeExtensions`、全部公开 `MigrationOperation`。报告逐项列出公开方法名和重载数量；同名重载共享一条兼容性结论，不重复展开每个泛型参数签名。
2. EF Core 官方源码标签 `v10.0.9`，提交 `6fecf7f6e78403c04ce8e127a0606336109d690c`。
3. Entity Framework 官方文档仓库，审计提交 `7ca120dcb01fa940686da9d3b9c8dd381a93e520`。
4. 仓库 `V9R1C10` 金仓官方 PDF 文档。
5. 当前 Provider 源码和真实数据库 Release 回归：51 个测试全部通过，其中 27 个为 Kdbndp 真实库集成测试。
6. 自动 API 契约测试反射锁定 .NET 10 的 63 个 `Queryable` 方法名称、EF Core 10 的 39 个 `EntityFrameworkQueryableExtensions` 名称和 6 个 `RelationalQueryableExtensions` 名称；新增或遗漏 API 会直接导致测试失败。

## 3. 已实库确认的能力

| 能力 | 状态 | 证据与说明 |
|---|---|---|
| `UseKdbndp(string)` | ✅ | Provider 可创建原生 `KdbndpConnection` |
| `UseKdbndp(DbConnection)` | ✅ | 可使用外部 Kdbndp 连接 |
| Oracle 兼容模式选项 | ✅ | `SetOracleCompatibilityMode()` 已进入 Provider 缓存键和日志 |
| 基础 `Where/Select/OrderBy/Take` | ✅ | 真实查询和 `ToQueryString` 已通过 |
| `LIMIT/OFFSET` | ✅ | 自定义 `KingbaseQuerySqlGenerator` |
| 新增/查询/修改/删除 | ✅ | Kdbndp 真实库 CRUD 回环通过 |
| 手工键值 Insert | ✅ | `ValueGeneratedNever` 已验证 |
| identity 主键回读 | ✅ | `GENERATED BY DEFAULT AS IDENTITY` 插入后成功回读 |
| 乐观并发更新条件 | ✅ | 并发字段参与 Update 条件；真实陈旧写入已触发并验证 `DbUpdateConcurrencyException` |
| `ExecuteDelete/ExecuteDeleteAsync` | ✅ | 真实库已验证简单谓词删除 |
| `Any/Single/ToList` 异步查询 | ✅ | 真实库已验证 |
| 基础事务性 `SaveChanges` | ✅ | 单命令批次成功提交 |
| `EnsureCreated` | ✅ | 基础表、列、主键 DDL 已验证 |
| `EnsureDeleted` | ✅ | 独立临时数据库删除与删除后不可连接已验证 |
| 数据库创建/删除 | ✅ | 通过可配置管理库 `UseAdminDatabase("template1")` 完成创建、建表、写入、删除闭环 |
| 数据库存在性/用户表检测 | ✅ | 已适配 Oracle 模式下 `sys*` 系统 schema 过滤 |
| 字符串专用翻译 | ✅ | `Contains(char)`、`StartsWith`、`EndsWith`、`ToLower/ToUpper`、`Substring`、`Replace`、`Trim`、`IndexOf`、`Length` 已实库验证 |
| 日期部件翻译 | ✅ | `DateTime.Year/Month/Day/Hour/Minute/Second` 已实库验证 |
| 数学函数翻译 | ✅ | `Abs/Ceiling/Floor/Round/Sqrt` 已实库验证 |
| 查询 API 契约 | ✅ | 63 个 `Queryable`、39 个 EF 查询扩展、6 个关系型查询扩展均有自动反射清单 |
| 聚合与集合运算 | ✅ | `All/Any/Average/Count/LongCount/Sum/Min/Max/Concat/Union/Intersect/Except/Distinct` 基础形态已实库验证 |
| .NET 10 左右连接 | ✅ | `LeftJoin` 已实库验证；`RightJoin` 过滤源通过 Provider 子查询下推修复并验证 |
| Split Query | ✅ | Provider 强制预缓冲，解决 Kdbndp 单连接不允许并发 Reader 的限制 |
| EF 查询扩展 | ✅ | 跟踪模式、异步终结、`ExecuteUpdate`、Include、Tag、Load、集合物化已验证 |
| 关系型查询扩展 | ✅ | `AsSingleQuery/AsSplitQuery/CreateDbCommand/FromSql/FromSqlInterpolated/FromSqlRaw` 已验证 |
| CLR 标量类型往返 | ✅ | 27 个标量/可空/枚举属性在同一实体中完成参数写入、数据库存储和读取验证 |
| 保存与批处理 | ✅ | 图新增、Range API、多命令批次、Attach、DetectChanges、默认/计算列、并发异常和级联删除已验证 |
| 设计时 Provider 发现 | ✅ | `DesignTimeProviderServices` 注册测试通过 |

## 4. EF Core 10 新增能力专项矩阵

| EF Core 10 新能力 | 72% - complex、DayNumber、集合参数和 NativeAOT 已验证；JSON 聚合模型仍缺 |
|---|---|---|
| Optional complex types - table splitting | ✅ | nullable complex、shadow discriminator、DDL、保存和查询已在真实库验证 |
| Complex types 映射 JSON | ❌ | 标量 JSON 已支持；complex/owned JSON 仍缺 EF JSON SQL tree、聚合 materializer 和 partial-update 管线 |
| Complex type struct | ✅ | struct complex 的持久化、读取和投影已实库验证 |
| Complex type structural equality | ✅ | complex 结构相等查询已实库验证 |
| Complex type bulk assignment | ✅ | complex 整体 `ExecuteUpdate` 已实库验证 |
| 参数化集合默认多参数翻译 | ✅ | 空集合和 128 元素参数集合均已实库验证 |
| `.NET 10 LeftJoin` | ✅ | 基础左连接和空值投影已在 Kingbase Oracle 模式实库验证 |
| `.NET 10 RightJoin` | ✅ | 基础右连接已验证；Provider 强制左源子查询，修复过滤条件被错误放入外层 `WHERE` 的语义问题 |
| Split query 稳定排序改进 | ✅ | `AsSplitQuery + Include + ThenInclude` 已实库验证；Provider 通过预缓冲兼容 Kdbndp 单活动 Reader 限制 |
| `DateOnly.ToDateTime` 翻译 | ✅ | 日期与时间相加翻译已加入真实库专项用例并通过 |
| `DateOnly.DayNumber` | ✅ | 翻译为日期减法并与 CLR 实库对照通过 |
| 微秒/纳秒 DatePart 翻译 | ✅ | `date_part` 与取模已验证；数据库微秒精度下 Nanosecond 为 0 |
| string 方法接收 `char` | ✅ | `Contains(char)` 已通过 `strpos` 在真实库验证 |
| JSON 列 `ExecuteUpdate` | ❌ | 无 JSON 映射和 JSON 更新 SQL |
| Named query filters | ✅ | 两个命名过滤器同时生效并完成实库验证；选择性禁用边界仍保留规格测试 |
| `ExecuteUpdateAsync` 普通 lambda | ✅ | 同步/异步普通标量表达式更新均已实库验证；JSON/复杂结构赋值另行列为不支持 |
| 参数名简化 | ✅ | EF Core 核心行为，已在生成 SQL 中观察到简化参数 |
| 原始 SQL 拼接分析器警告 | ✅ | 编译期 EF Core 分析器能力，与数据库 Provider 无关 |
| 内联常量日志脱敏 | ✅ | EF Core 日志能力，与 Kingbase SQL 方言无关 |

## 5. 全部 `Queryable` 标准运算符兼容矩阵

下表列出 .NET 10 `System.Linq.Queryable` 的全部公开方法名称。状态指服务端 EF 查询，不是 `AsEnumerable()` 后的客户端 LINQ。

| 方法 | 状态 | 说明 |
|---|---|---|
| `Aggregate` | ⛔ | 任意 accumulator 表达式没有 EF Core 关系型服务端语义，不是 Kingbase 方言缺口 |
| `AggregateBy` | ⛔ | .NET 10 API 未进入 EF Core 10 关系型查询翻译管线 |
| `All` | ✅ | `AllAsync` 已在真实库验证 |
| `Any` | ✅ | `AnyAsync` 已在真实库验证 |
| `Append` | ⛔ | EF Core 10 无关系型服务端翻译入口 |
| `AsQueryable` | ✅ | 不改变 SQL，属于 LINQ 管线操作 |
| `Average` | ✅ | `double` 基础聚合已实库验证；其他数值重载由 API 契约覆盖、边界精度仍需规格测试 |
| `Cast` | ✅ | 标量 object-to-int 查询形态已实库验证；其他数据库类型转换边界仍由类型映射规范测试覆盖 |
| `Chunk` | ⛔ | EF Core 10 无关系型服务端翻译入口 |
| `Concat` | ✅ | 标量 `UNION ALL` 结果已实库验证 |
| `Contains` | ✅ | 标量子查询 `Contains/ContainsAsync` 已实库验证；实体和大参数集合边界仍待规格测试 |
| `Count` | ✅ | `CountAsync` 已实库验证 |
| `CountBy` | ⛔ | .NET 10 API 未进入 EF Core 10 关系型查询翻译管线 |
| `DefaultIfEmpty` | ✅ | 空标量序列返回 CLR 默认值已实库验证，LeftJoin 组合也已验证 |
| `Distinct` | ✅ | 标量基础 `DISTINCT` 已实库验证 |
| `DistinctBy` | ⛔ | EF Core 10 无关系型服务端翻译入口 |
| `ElementAt` | ✅ | 确定排序后的 `ElementAtAsync` 已实库验证 |
| `ElementAtOrDefault` | ✅ | 确定排序后的异步形态已实库验证 |
| `Except` | ✅ | 标量基础集合运算已实库验证 |
| `ExceptBy` | ⛔ | EF Core 10 无关系型服务端翻译入口 |
| `First` | ✅ | `FirstAsync` 已实库验证 |
| `FirstOrDefault` | ✅ | 谓词异步形态已实库验证 |
| `GroupBy` | ✅ | 简单 key、`Count`、`Sum` 投影已实库验证；返回完整分组和复杂导航仍待规格测试 |
| `GroupJoin` | 🟠 | `GroupJoin + Count` 聚合投影已实库验证；直接返回嵌套分组集合仍没有单条关系型结果集语义 |
| `Index` | ⛔ | .NET 10 API 未进入 EF Core 10 关系型查询翻译管线 |
| `Intersect` | ✅ | 标量基础集合运算已实库验证 |
| `IntersectBy` | ⛔ | EF Core 10 无关系型服务端翻译入口 |
| `Join` | ✅ | 基础 Inner Join 已实库验证 |
| `Last` | ✅ | 确定排序后的 `LastAsync` 已实库验证 |
| `LastOrDefault` | ✅ | 确定排序后的异步形态已实库验证 |
| `LeftJoin` | ✅ | 基础 key、空值投影和结果顺序已实库验证 |
| `LongCount` | ✅ | `LongCountAsync` 已实库验证 |
| `Max` | ✅ | `double` 聚合已实库验证 |
| `MaxBy` | ⛔ | EF Core 10 无关系型服务端翻译入口 |
| `Min` | ✅ | `double` 聚合已实库验证 |
| `MinBy` | ⛔ | EF Core 10 无关系型服务端翻译入口 |
| `OfType` | ✅ | 同实体类型以及 TPH/TPT/TPC 派生类型查询均已实库验证 |
| `Order` | ✅ | 标量序列默认升序已实库验证；实体默认比较器仍不具备 SQL 语义 |
| `OrderBy` | ✅ | 真实库已验证 |
| `OrderByDescending` | ✅ | 基础降序排序已实库验证 |
| `OrderDescending` | ✅ | 标量序列默认降序已实库验证 |
| `Prepend` | ⛔ | EF Core 10 无关系型服务端翻译入口 |
| `Reverse` | ✅ | 已排序查询反向排序已实库验证；无排序序列仍没有确定语义 |
| `RightJoin` | ✅ | 基础语义已验证；Provider 对左源强制子查询以保持过滤条件语义 |
| `Select` | ✅ | 实体和标量投影已验证；复杂结构投影仍未覆盖 |
| `SelectMany` | ✅ | 非相关 Cross Join、相关 `JOIN LATERAL` 和 `LEFT JOIN LATERAL` 均已实库验证 |
| `SequenceEqual` | ⛔ | EF Core 10 无查询序列整体比较的关系型翻译入口；`byte[]` 标量比较是另一能力 |
| `Shuffle` | ⛔ | .NET 10 API 未进入 EF Core 10 关系型查询翻译管线 |
| `Single` | ✅ | `SingleAsync` 已真实验证 |
| `SingleOrDefault` | ✅ | 空结果异步形态已实库验证 |
| `Skip` | ✅ | `OFFSET + LIMIT` 已实库验证 |
| `SkipLast` | ⛔ | EF Core 10 无关系型服务端翻译入口 |
| `SkipWhile` | ⛔ | EF Core 虽有抽象翻译钩子，但关系型实现不提供通用有序前缀语义 |
| `Sum` | ✅ | `double` 聚合已实库验证 |
| `Take` | ✅ | 参数化 `LIMIT` 已真实验证 |
| `TakeLast` | ⛔ | EF Core 10 无关系型服务端翻译入口 |
| `TakeWhile` | ⛔ | EF Core 虽有抽象翻译钩子，但关系型实现不提供通用有序前缀语义 |
| `ThenBy` | ✅ | 复合升序排序已实库验证 |
| `ThenByDescending` | ✅ | 复合降序排序已实库验证 |
| `Union` | ✅ | 标量基础 `UNION` 已实库验证 |
| `UnionBy` | ⛔ | EF Core 10 无关系型服务端翻译入口 |
| `Where` | ✅ | 比较谓词已真实验证 |
| `Zip` | ⛔ | EF Core 10 无关系型服务端翻译入口 |

## 6. EF Core 查询扩展 API 全清单

### 6.1 `EntityFrameworkQueryableExtensions`

| API | 状态 | 说明 |
|---|---|---|
| `AllAsync` | ✅ | 已实库验证 |
| `AnyAsync` | ✅ | 已验证 |
| `AsAsyncEnumerable` | ✅ | 查询结果异步枚举；数据库执行能力取决于原查询 |
| `AsNoTracking` | ✅ | 与查询执行组合已验证 |
| `AsNoTrackingWithIdentityResolution` | ✅ | 与查询执行组合已验证 |
| `AsTracking` | ✅ | 默认跟踪 CRUD 已验证 |
| `AverageAsync` | ✅ | `double` 选择器已实库验证 |
| `ContainsAsync` | ✅ | 标量子查询已实库验证 |
| `CountAsync` | ✅ | 已实库验证 |
| `ElementAtAsync` | ✅ | 确定排序形态已实库验证 |
| `ElementAtOrDefaultAsync` | ✅ | 确定排序形态已实库验证 |
| `ExecuteDelete` | ✅ | 简单谓词已验证 |
| `ExecuteDeleteAsync` | ✅ | 已验证 |
| `ExecuteUpdate` | ✅ | 同步标量表达式更新已实库验证 |
| `ExecuteUpdateAsync` | ✅ | 异步字符串表达式更新已实库验证 |
| `FirstAsync` | ✅ | 已实库验证 |
| `FirstOrDefaultAsync` | ✅ | 谓词形态已实库验证 |
| `ForEachAsync` | ✅ | 已实库验证 |
| `IgnoreAutoIncludes` | ✅ | 与真实查询组合验证；复杂 AutoInclude 图仍待规格测试 |
| `IgnoreQueryFilters` | ✅ | API 与真实查询组合验证；命名过滤器选择性禁用仍待专项测试 |
| `Include` | ✅ | 自引用一对多 Include 已实库验证 |
| `LastAsync` | ✅ | 确定排序形态已实库验证 |
| `LastOrDefaultAsync` | ✅ | 确定排序形态已实库验证 |
| `Load` | ✅ | 同步跟踪填充已实库验证 |
| `LoadAsync` | ✅ | 异步跟踪填充已实库验证 |
| `LongCountAsync` | ✅ | 已实库验证 |
| `MaxAsync` | ✅ | `double` 选择器已实库验证 |
| `MinAsync` | ✅ | `double` 选择器已实库验证 |
| `SingleAsync` | ✅ | 已验证 |
| `SingleOrDefaultAsync` | ✅ | 空结果形态已实库验证 |
| `SumAsync` | ✅ | `double` 选择器已实库验证 |
| `TagWith` | ✅ | SQL 注释生成和真实执行已验证 |
| `TagWithCallSite` | ✅ | 与真实查询组合验证 |
| `ThenInclude` | ✅ | 向下两层导航已实库验证 |
| `ToArrayAsync` | ✅ | 已实库验证 |
| `ToDictionaryAsync` | ✅ | 已实库验证 |
| `ToHashSetAsync` | ✅ | 已实库验证 |
| `ToListAsync` | ✅ | 已验证 |
| `ToQueryString` | ✅ | 已验证 |

### 6.2 `RelationalQueryableExtensions`

| API | 状态 | 说明 |
|---|---|---|
| `AsSingleQuery` | ✅ | Include 单查询已实库验证 |
| `AsSplitQuery` | ✅ | Include/ThenInclude 多命令已实库验证；Provider 强制预缓冲以兼容 Kdbndp |
| `CreateDbCommand` | ✅ | 已验证返回原生 `KdbndpCommand` 和正确 SQL |
| `FromSql` | ✅ | 可组合实体查询已实库验证 |
| `FromSqlInterpolated` | ✅ | 已实库验证 |
| `FromSqlRaw` | ✅ | 已实库验证；调用者仍必须自行防 SQL 注入 |

## 7. 表达式、运算符和函数翻译

| 语法族 | 状态 | 当前情况 |
|---|---|---|
| `== != > >= < <=` | ✅ | 整数、decimal、double 和字符串谓词已实库验证 |
| `&& || !` | ✅ | 组合布尔谓词和否定表达式已实库验证 |
| `+ - * / %` | ✅ | decimal 四则运算和整数取模已实库验证；极值溢出仍由 CLR/数据库类型边界决定 |
| 三元条件 `?:` | ✅ | `CASE` 投影已实库验证 |
| null 合并 `??` | ✅ | 可空整数 `COALESCE` 已在 Oracle 兼容模式实库验证 |
| null 比较与补偿 | ✅ | nullable 比较与 SQL null compensation 已实库验证 |
| 字符串拼接 | ✅ | Provider 将字符串加法输出为 `||`，已实库验证 |
| `string.Equals` | ✅ | 实例 Equals 已实库验证 |
| `Contains/StartsWith/EndsWith` | ✅ | 使用 `strpos/left/right` 专用翻译，真实库语义测试通过 |
| `ToLower/ToUpper` | ✅ | 使用 `lower/upper`，真实库验证通过 |
| `Substring/Replace/Trim/IndexOf` | ✅ | 使用 `substr/replace/btrim/strpos` 并处理 .NET 零基索引，真实库验证通过 |
| `string.Length` | ✅ | 使用 `length`，真实库验证通过 |
| `EF.Functions.Like` | ✅ | 基础模式匹配已实库验证；用户提供模式和转义字符时仍需遵守数据库规则 |
| `EF.Functions.Collate` | ✅ | `c` 排序规则比较已实库验证 |
| `EF.Functions.Random` | ✅ | 修正 EF 默认 `rand()` 为 Kingbase `random()` 并实库验证返回范围 |
| `Math.*` | 🟠 | 已实现并验证 abs、ceil、floor、round、sqrt、cbrt、log/log2、max/min/clamp、三角及双曲函数；无单值 SQL 等价物的方法仍不翻译 |
| `DateTime.*` 成员/方法 | ✅ | 日期部件、AddYears/Months/Days/Hours/Minutes/Seconds/毫秒/微秒、Now/UtcNow/Today 已实库验证 |
| `DateOnly/TimeOnly` 方法 | ✅ | 日期/时间部件、AddYears/Months/Days/Hours/Minutes、FromDateTime 和 ToDateTime 已实现并实库验证 |
| `Guid.NewGuid()` | ✅ | 使用 `CAST(sys_guid() AS uuid)`，返回 Guid 已实库验证 |
| Enum `HasFlag/ToString` | ✅ | `HasFlag` 与整数枚举 `ToString` 均已实库验证 |
| `Regex` | ✅ | `Regex.IsMatch` 基础和 RegexOptions 标志通过 `regexp_like` 翻译并实库验证 |
| byte array `SequenceEqual` | ✅ | 列对列和内联 byte[] 均使用 `bytea` mapping 并实库验证 |
| 全文检索 | ✅ | `tsvector/tsquery` 映射及 `FullTextMatches` 已实库验证 |
| 数组运算 | ✅ | `int[]/List<int>` 往返及 contains/contained/overlap/cardinality 已实库验证 |
| range 运算 | ✅ | `KdbndpRange<int>` 往返及 contains/overlap 已实库验证 |
| JSON path/query | 🟠 | 标量 JSON 单层提取已验证；任意 path、owned/complex JSON 和 partial update 仍不支持 |
| 窗口函数 | ❌ | 数据库 SQL 支持，但 EF Core 10 无通用公开窗口表达式入口；需专用节点和排序 API |
| LATERAL/APPLY | ✅ | `CROSS APPLY/OUTER APPLY` 已改写为 `JOIN LATERAL/LEFT JOIN LATERAL ... ON TRUE` 并实库验证 |

## 8. 模型构建语法矩阵

| 模型语法 | 状态 | 说明 |
|---|---|---|
| `DbSet<TEntity>` / `Entity<TEntity>` | ✅ | 已验证 |
| 表名、schema、列名 | ✅ | 默认 schema、显式 schema、表名和列名均通过 Provider DDL 和实库查询验证 |
| 主键、复合主键 | ✅ | 单主键及复合主键保存/查询已实库验证 |
| Alternate Key / Unique Constraint | ✅ | Alternate Key DDL 和关系模型已实库创建 |
| Keyless Entity | ✅ | `ToView` 和 `ToSqlQuery` 均已实库查询验证 |
| Required/Optional 属性 | ✅ | required、nullable 引用和 nullable 标量均已实库验证 |
| Shadow Property | ✅ | Shadow 属性建列、写入、读取和跟踪已实库验证 |
| Indexer Property / Property Bag | ✅ | shared-type Dictionary property bag 已建表、保存和查询验证 |
| Backing Field / Field-only | ✅ | backing field 访问模式已保存和回读验证 |
| Column Type/Name/Order | ✅ | 显式类型、列名和列顺序已通过生成 DDL 验证 |
| MaxLength / Unicode | ✅ | `character varying(n)` 等 facet 已通过真实 DDL、保存和查询验证 |
| Precision/Scale | ✅ | `numeric(18,4)` DDL、保存及计算列精度已验证 |
| Default Value / Default SQL | ✅ | 常量默认值和默认 SQL 均已回读验证 |
| Computed Column | ✅ | 实现 `GENERATED ALWAYS AS (...) STORED` DDL 并完成回读验证 |
| ValueGeneratedNever | ✅ | 已验证 |
| ValueGeneratedOnAdd identity | ✅ | 已验证 |
| Sequence | ✅ | schema 内序列 DDL、默认 `nextval` 和回读已实库验证 |
| HiLo | ❌ | 无 Kingbase HiLo/value generator 实现 |
| GUID 数据库生成 | ✅ | `CAST(sys_guid() AS uuid)` 默认 SQL 已实库生成和回读 |
| Concurrency Token | ✅ | 更新条件已验证 |
| RowVersion/Timestamp | ❌ | 无 Kingbase pg/oracle 模式统一 rowversion 实现 |
| Check Constraint | ✅ | Check Constraint DDL 已创建并通过系统目录确认 |
| Index：普通/唯一/复合 | ✅ | 普通、唯一和复合索引已通过 DDL 与系统目录验证 |
| Index：descending/filter/include | ✅ | descending、filter 和 `IncludeProperties` 均已真实 DDL 验证 |
| 一对一 | ✅ | 复合外键一对一、Include 和 Restrict 行为已验证 |
| 一对多 | ✅ | Include、关系修复和数据库 FK 级联删除已实库验证 |
| 多对多/Skip Navigation | ✅ | 隐式连接表创建、保存和 Include 已实库验证 |
| 自引用关系 | ✅ | 自引用父子关系和删除后 SetNull 已实库验证 |
| Cascade/Restrict/SetNull | ✅ | Cascade、Restrict 和 SetNull 三种行为均已实库验证 |
| Owned Entity | ✅ | 同表 Owned 和独立表 Owned 保存/查询已验证；JSON owned 仍不支持 |
| Complex Type table splitting | ✅ | 关系型 Complex Property 列拆分保存和回读已实库验证 |
| Complex Type JSON | ❌ | 不支持 |
| Entity/Table Splitting | ✅ | `SplitToTable` 双表保存和查询已实库验证 |
| TPH inheritance | ✅ | discriminator、派生类型保存和查询已实库验证 |
| TPT inheritance | ✅ | 多表保存和 `OfType` 查询已实库验证 |
| TPC inheritance | ✅ | 多表保存、Union 查询和派生筛选已实库验证 |
| Discriminator | ✅ | TPH 字符串 discriminator 已实库验证 |
| Global Query Filter | ✅ | 全局过滤器和 IgnoreQueryFilters 已实库验证 |
| Named Query Filters (EF10) | ✅ | 两个命名过滤器同时生效并完成实库验证 |
| Value Converter | ✅ | Enum-to-string converter 已保存和回读验证 |
| Value Comparer | ✅ | 自定义 ValueComparer 已进入模型和跟踪链路验证 |
| Data Seeding `HasData` | ✅ | CreateTables 数据种子已实库查询验证 |
| `UseSeeding/UseAsyncSeeding` | ✅ | 独立临时数据库中同步与异步 seeding 已验证 |
| Database Function mapping | ✅ | `HasDbFunction` 映射 `pg_catalog.upper` 已实库执行 |
| View mapping | ✅ | 真实 View 的 `ToView` 和 `ToSqlQuery` 已执行验证 |
| Stored procedure CUD mapping | ❌ | 无 Kingbase 存储过程更新 SQL 实现 |
| Temporal Table | 🚫 | SQL Server 专有；Kingbase 需另行设计 |
| Spatial/NetTopologySuite | ❌ | 无空间扩展包和类型映射 |
| JSON document model | 🟠 | 标量 `JsonDocument/JsonElement` 已支持；owned/complex JSON 文档模型仍不支持 |
| Primitive Collections | 🟠 | `int[]/List<int>` 的 `integer[]` 往返及专用函数已验证；任意元素类型未覆盖 |

## 9. CLR 与数据库类型支持

| CLR 类型 | Store Type | 状态 |
|---|---|---|
| `bool` | `boolean` | ✅，实库往返通过 |
| `byte` | `smallint` | ✅，包含 250 边界值 |
| `short` | `smallint` | ✅，负值往返通过 |
| `int` | `integer` | ✅ |
| `long` | `bigint` | ✅，大负值往返通过 |
| `float` | `real` | ✅ |
| `double` | `double precision` | ✅ |
| `decimal` | `numeric` / `numeric(p,s)` | ✅，`numeric(18,4)` DDL、参数和精度往返通过 |
| `string` | `text` / `varchar(n)` / `char(n)` | ✅，Unicode、变长和定长字符串已验证 |
| `Guid` | `uuid` | ✅，实库往返通过 |
| `byte[]` | `bytea` | ✅，包含 0/128/255 字节值 |
| `DateOnly` | `date` | ✅，Provider 内置 `DateOnly ↔ DateTime` 驱动转换并实库验证 |
| `TimeOnly` | `time without time zone` | ✅，Provider 内置 `TimeOnly ↔ TimeSpan` 驱动转换并实库验证 |
| `TimeSpan` | `interval` | ✅，改用 Kdbndp 支持的对象参数，超过 24 小时 interval 往返通过 |
| `DateTime` | `timestamp without time zone` | ✅，毫秒精度和 `Unspecified` 策略已实库验证 |
| `DateTimeOffset` | `timestamp with time zone` | ✅，Oracle 模式下按 UTC 时刻一致性验证 |
| Nullable 上述类型 | 对应类型 | ✅，null 写入和读取通过 |
| `char` | `character(1)` | ✅，Unicode 字符往返通过 |
| unsigned integers | `smallint/integer/bigint/numeric(20,0)` | ✅，`sbyte/ushort/uint/ulong` 均已验证；参数 DbType 已适配 Kdbndp |
| enum | `integer` | ✅，默认整数转换实库往返通过 |
| `JsonDocument/JsonElement` | `jsonb` | ✅，写入、读取、快照 comparer 和提取函数已验证 |
| `int[]` / `List<int>` | `integer[]` | ✅，结构 comparer、element mapping、参数与列往返已验证 |
| `KdbndpRange<int>` | `int4range` | ✅，参数、读取和函数已验证 |
| Spatial 类型 | - | ❌，当前实例不存在空间扩展，无法完成实库验收 |
| NodaTime 类型 | - | ❌，需 NodaTime 包及 Kdbndp 对应插件，应独立交付 |

## 10. 保存、跟踪和批量操作

| API/语法 | 状态 | 说明 |
|---|---|---|
| `Add/AddAsync/AddRange` | ✅ | 单实体、图新增和多个根实体 Range 已实库验证 |
| `Attach/AttachRange` | ✅ | 多实体附加、Unchanged 状态和后续更新已验证 |
| `Update/UpdateRange` | ✅ | 跟踪更新和两个实体同批 UpdateRange 已验证 |
| `Remove/RemoveRange` | ✅ | 多实体删除和关联子项数据库级联已验证 |
| `SaveChanges/SaveChangesAsync` | ✅ | 同步、异步、单命令和多命令链路均已验证 |
| ChangeTracker DetectChanges | ✅ | 关闭自动检测后手工 `DetectChanges` 状态转换和保存已验证 |
| Snapshot/Notification tracking | ✅ | Snapshot 与通知跟踪实体均已验证 |
| Identity Resolution | ✅ | Include 查询中同一父实体实例复用已验证 |
| Relationship fixup | ✅ | 新增图和查询物化后的双向导航修复已验证 |
| Temporary values | ✅ | identity 插入和数据库生成键回填链路已验证 |
| Store-generated identity | ✅ | 已验证 |
| Store-generated default/computed | ✅ | `DEFAULT 42` 和 stored generated column 均已回读验证 |
| Optimistic concurrency success | ✅ | 并发字段 Update 已通过 |
| `DbUpdateConcurrencyException` | ✅ | 双 Context stale writer 零影响行数冲突已验证 |
| Cascade delete client/database | ✅ | FK `ON DELETE CASCADE` 删除父图后子表为空已验证 |
| Batching | ✅ | 已实现 `KingbaseModificationCommandBatch`，多 Insert/Update/Delete 命令同批执行通过 |
| `ExecuteDelete` | ✅ | 简单单表谓词已验证 |
| `ExecuteUpdate` | ✅ | 同步和异步简单标量表达式更新均已实库验证；JSON/复杂结构赋值另行列为不支持 |
| JSON `ExecuteUpdate` | ❌ | 不支持 |
| Stored procedure SaveChanges | ❌ | 不支持 |

## 11. 连接、事务、原始 SQL 和执行策略

| API | 状态 | 说明 |
|---|---|---|
| `OpenConnection/CloseConnection` 同步/异步 | ✅ | 内部连接与外部 `KdbndpConnection` 生命周期已实库验证 |
| `BeginTransaction/Commit/Rollback` | ✅ | 同步回滚、异步提交、`SaveChanges`/原始 SQL 事务均已实库验证 |
| Savepoint | ✅ | `CreateSavepointAsync`、`RollbackToSavepointAsync` 和提交后数据状态已实库验证 |
| Ambient `TransactionScope` | ✅ | 单库环境下启用异步流并完成提交，数据可见性已实库验证 |
| `UseTransaction` | ✅ | 外部 Kdbndp 事务注入、执行、回滚和解除绑定已实库验证 |
| Execution Strategy/Retry | ✅ | 新增 `EnableRetryOnFailure`、`KingbaseExecutionStrategy` 和 factory；识别 `KdbndpException.IsTransient`、`KingbaseException.IsTransient`、附加 SQLSTATE 与 `TimeoutException`，首次超时后二次成功已验证 |
| Command timeout | ✅ | `SetCommandTimeout(TimeSpan)` 与 `GetCommandTimeout` 下传/读取已验证 |
| Connection pooling | ✅ | 小池 4 下 40 路并发通过，并新增 `KdbndpDataSource` 接入 |
| 多主机/故障转移 | 🟠 | `KdbndpMultiHostDataSource` 已接入并单节点验证；真实切换需要多节点 |
| `FromSql/FromSqlRaw/FromSqlInterpolated` | ✅ | 三种实体原始 SQL 查询均已实库验证 |
| `SqlQuery/SqlQueryRaw` | ✅ | 参数化与原始标量查询均已实库验证 |
| `ExecuteSql/ExecuteSqlRaw/Interpolated` | ✅ | 同步/异步、参数化/原始命令基础形态均已实库验证 |
| Query/Command/Transaction Interceptor | ✅ | Command、QueryExpression、Transaction/savepoint 均专项验证 |
| Compiled Query | ✅ | `EF.CompileAsyncQuery` 已在真实库执行并返回正确结果 |
| Compiled Model | ✅ | 已注册 Kingbase runtime annotation generator，`dotnet ef dbcontext optimize` 10.0.9 成功生成 4 个 compiled model 文件 |
| Query precompilation/NativeAOT | ✅ | optimize、win-x64 AOT publish 和原生进程真实查询通过；EF Core 仍标注 experimental |

## 12. `RelationalDatabaseFacadeExtensions` 全清单

| API | 状态 | 说明 |
|---|---|---|
| `BeginTransaction` | ✅ | 同步事务、保存和显式回滚已实库验证 |
| `BeginTransactionAsync` | ✅ | 异步事务、保存和显式提交已实库验证 |
| `CloseConnection` | ✅ | 真实 Kdbndp 内部连接生命周期已覆盖 |
| `CloseConnectionAsync` | ✅ | 异步关闭后状态为 `Closed` 已验证 |
| `ExecuteSql` | ✅ | 参数化 `FormattableString` 非查询命令已实库验证 |
| `ExecuteSqlAsync` | ✅ | 参数化异步非查询命令已实库验证 |
| `ExecuteSqlInterpolated` | ✅ | 与 `ExecuteSql` 共用 EF 参数化管线；参数类型和结果行数已覆盖 |
| `ExecuteSqlInterpolatedAsync` | ✅ | 异步参数化执行已实库验证 |
| `ExecuteSqlRaw` | ✅ | 同步原始 SQL 与对象参数重载已实库验证 |
| `ExecuteSqlRawAsync` | ✅ | 异步 DDL/DML 和参数重载已实库验证 |
| `GenerateCreateScript` | ✅ | 当前模型成功生成 Kingbase 建表脚本并包含 `CREATE TABLE` |
| `GetAppliedMigrations` | ✅ | `KingbaseHistoryRepository` 查询迁移历史，升级/回滚状态已验证 |
| `GetAppliedMigrationsAsync` | ✅ | 异步历史查询已实库验证 |
| `GetCommandTimeout` | ✅ | 与设置值一致 |
| `GetConnectionString` | ✅ | Provider 连接字符串配置链路已验证 |
| `GetDbConnection` | ✅ | 返回原生 `KdbndpConnection` |
| `GetMigrations` | ✅ | 程序内与 `dotnet ef migrations list` 均识别两个迁移 |
| `GetPendingMigrations` | ✅ | 历史表为空、全部应用和全部回滚三种状态已验证 |
| `GetPendingMigrationsAsync` | ✅ | 异步待处理迁移查询已验证 |
| `HasPendingModelChanges` | ✅ | `dotnet ef database update` 能检测模型与 snapshot 差异并继续执行迁移 |
| `IsRelational` | ✅ | Provider 已注册关系型服务，返回 `true` |
| `Migrate` | ✅ | `dotnet ef database update` 10.0.9 已完成两级升级 |
| `MigrateAsync` | ✅ | `Database.MigrateAsync` 已完成升级、指定目标回滚和回滚到 `0` |
| `OpenConnection` | ✅ | 真实 Kdbndp 同步连接已验证 |
| `OpenConnectionAsync` | ✅ | 异步连接状态已验证 |
| `SetCommandTimeout` | ✅ | `TimeSpan` 设置已验证；EF 关系型重载共享同一状态 |
| `SetConnectionString` | ✅ | 打开前、打开时和关闭后替换边界均专项验证 |
| `SetDbConnection` | ✅ | 外部 `KdbndpConnection` 注入已验证 |
| `SqlQuery` | ✅ | 参数化标量 SQL 查询已实库验证 |
| `SqlQueryRaw` | ✅ | 原始标量 SQL 查询已实库验证 |
| `UseTransaction` | ✅ | 外部事务归属、回滚和解绑语义已验证 |
| `UseTransactionAsync` | ✅ | 异步外部事务链路已实库验证 |

## 13. Migration 全部操作类型兼容矩阵

> 已新增 Kingbase 专用 `IHistoryRepository`、显式 advisory migration lock、幂等条件块和方言 SQL generator。除数据库本身不允许在线修改数据库排序规则的 `AlterDatabaseOperation` 外，EF Core 10 公开 MigrationOperation 均已有生成链路；下表标明真实执行覆盖范围。

| MigrationOperation | 状态 | 说明 |
|---|---|---|
| `AddCheckConstraintOperation` | ✅ | 建表内与 standalone check constraint 已实库执行 |
| `AddColumnOperation` | ✅ | nullable 列和带默认值 required 列已执行 |
| `AddForeignKeyOperation` | ✅ | 建表内与 standalone FK、Cascade 已验证 |
| `AddPrimaryKeyOperation` | ✅ | 建表内与 drop 后重新添加均已验证 |
| `AddUniqueConstraintOperation` | ✅ | 建表内与 drop 后重新添加均已验证 |
| `AlterColumnOperation` | ✅ | 类型、nullable→required、NULL 回填和默认值设置已验证 |
| `AlterDatabaseOperation` | ❌ | KingbaseES/PostgreSQL 系数据库不能在现有数据库上在线修改 collation；Provider 明确保留不支持，而不是生成无效 SQL |
| `AlterSequenceOperation` | ✅ | increment 变更已实库验证 |
| `AlterTableOperation` | ✅ | `COMMENT ON TABLE` 添加/变更注释已实库验证 |
| `CreateIndexOperation` | ✅ | 普通、唯一、复合、descending、filtered index 已验证 |
| `CreateSequenceOperation` | ✅ | start/increment 已实库验证 |
| `CreateTableOperation` | ✅ | 列、主键、唯一键、check、FK 和 schema 表已验证 |
| `DeleteDataOperation` | ✅ | 显式 column type 的 seed delete SQL 已执行 |
| `DropCheckConstraintOperation` | ✅ | standalone check constraint 删除已执行 |
| `DropColumnOperation` | ✅ | 重命名后的列删除已执行 |
| `DropForeignKeyOperation` | ✅ | standalone FK 删除后重建已执行 |
| `DropIndexOperation` | ✅ | Kingbase 专用 `DROP INDEX` 实现，迁移回滚和 standalone 删除均已验证 |
| `DropPrimaryKeyOperation` | ✅ | standalone 主键删除后重建已执行 |
| `DropSchemaOperation` | ✅ | 独立 schema 中表清理后删除 schema 已执行 |
| `DropSequenceOperation` | ✅ | 重命名后的序列删除已执行 |
| `DropTableOperation` | ✅ | 迁移 Down 和独立 schema 表删除已验证 |
| `DropUniqueConstraintOperation` | ✅ | standalone 唯一约束删除后重建已执行 |
| `EnsureSchemaOperation` | ✅ | `CREATE SCHEMA IF NOT EXISTS` 与 schema 内建表已验证 |
| `InsertDataOperation` | ✅ | 显式 column type 的 seed insert SQL 已执行 |
| `RenameColumnOperation` | ✅ | `ALTER TABLE ... RENAME COLUMN` 已执行 |
| `RenameIndexOperation` | ✅ | `ALTER INDEX ... RENAME TO` 已执行 |
| `RenameSequenceOperation` | ✅ | `ALTER SEQUENCE ... RENAME TO` 已执行 |
| `RenameTableOperation` | ✅ | schema 内表重命名后继续删除已验证；跨 schema 使用 `SET SCHEMA` 生成链已实现 |
| `RestartSequenceOperation` | ✅ | `RESTART WITH` 已实库执行 |
| `SqlOperation` | ✅ | 自动补齐 statement terminator；普通迁移与幂等块中均已执行 |
| `UpdateDataOperation` | ✅ | 显式 key/column type 的 seed update SQL 已执行 |

### Migration 端到端状态

- ✅ `KingbaseHistoryRepository`：创建/检测 `__EFMigrationsHistory`、同步/异步 applied/pending 查询均已验证。
- ✅ `dotnet ef migrations add`：在隔离临时 .NET 10 项目中成功生成 migration、designer 和 snapshot 三个文件。
- ✅ `dotnet ef database update`：两级升级和回滚到 `0` 均已真实执行。
- ✅ `dotnet ef migrations script --idempotent`：脚本已生成，并在同一真实库连续执行两次，第二次无重复 DDL/DML。
- ✅ 模式迁移锁：使用 `pg_advisory_lock/pg_advisory_unlock`，同步/异步迁移流程已实际经过锁链路。
- ✅ 创建/删除数据库：独立临时库闭环已验证；管理库可由 `UseAdminDatabase(...)` 配置。
- 🟠 限制：`AlterDatabaseOperation` 的现有数据库 collation 变更受数据库能力限制；跨 schema rename、超大迁移和失败恢复仍需更高强度压力测试。

## 14. Database First、设计时和工具链

| 能力 | 状态 | 说明 |
|---|---|---|
| `dotnet ef` 识别 Provider | ✅ | `DesignTimeProviderServices` 与 dotnet-ef 10.0.9 已验证 |
| `dotnet ef migrations add` | ✅ | 隔离临时项目成功生成 migration/designer/snapshot |
| `dotnet ef database update` | ✅ | 真实 Kingbase 数据库两级升级与回滚到 `0` 成功 |
| `dotnet ef migrations script` | ✅ | 普通/幂等脚本生成；幂等脚本连续执行两次通过 |
| `dotnet ef dbcontext scaffold` | ✅ | CLI 成功生成 Context 和实体类 |
| Provider 配置代码生成 | ✅ | `KingbaseProviderCodeGenerator` 生成 `UseKdbndp(...)`，CLI 输出已验证 |
| Annotation C# 代码生成 | ✅ | `KingbaseAnnotationCodeGenerator` 已注册，migration designer/scaffold 代码生成成功 |
| Reverse engineer table/view/key/index | ✅ | 表、视图、列、PK、唯一约束、descending index、FK/Cascade 已真实反向工程 |
| Reverse engineer sequence | ✅ | `integer/bigint` 反向类型优先映射到 `int/long`，CLI 生成 `HasSequence` 已验证 |
| Compiled model optimize | ✅ | `KingbaseCSharpRuntimeAnnotationCodeGenerator` 已注册，CLI 成功生成 4 个 compiled model 文件 |
| NativeAOT optimize/precompiled queries | ✅ | `tools/Kingbase.NativeAotProbe` 已生成模型/interceptor、AOT 发布并真实运行 |

## 15. 其他数据库 Provider 专有语法

以下不是“EF Core 公共语法”，不能要求 Kingbase 原样兼容：

| 专有能力 | 结论 |
|---|---|
| SQL Server temporal table、HierarchyId、Azure vector、SQL Server JSON `modify` | 🚫 不适用，需要 Kingbase 等价扩展另行设计 |
| Npgsql `ILike`、数组、range、PostgreSQL enum、全文检索、NodaTime | 🚫 API 不直接兼容；可以参考并实现 Kingbase 扩展 |
| SQLite FTS、JSON、AUTOINCREMENT 专有扩展 | 🚫 不适用 |
| Cosmos partition key、vector、full-text、document patch | 🚫 非关系型 Provider，不适用 |

## 16. 当前最明确的不支持清单

1. **EF JSON 聚合模型**：complex/owned JSON、任意 JSON path、partial update 与 JSON `ExecuteUpdate` 需要 EF JSON SQL tree、materializer 和 update pipeline。
2. **Stored procedure CUD/SaveChanges**：需要修改命令批次生成，并处理结果列和并发返回值。
3. **HiLo**：需要 Kingbase sequence HiLo value-generator factory、注解和模型约定；普通 sequence 支持不等于 HiLo。
4. **统一 rowversion**：Kingbase Oracle 模式没有 SQL Server `rowversion` 等价类型；当前仅支持普通 concurrency token。
5. **Spatial/NetTopologySuite**：当前实例未安装空间扩展，`postgis_version()` 不存在。
6. **NodaTime**：需要 NodaTime 包及 Kdbndp 对应插件/转换器，应作为独立扩展包。
7. **窗口函数 API**：数据库支持窗口 SQL，但 EF Core 10 无通用公开表达式入口。
8. **Queryable 无服务端语义项**：`Aggregate/AggregateBy/Append/Chunk/CountBy/DistinctBy/ExceptBy/Index/IntersectBy/MaxBy/MinBy/Prepend/SequenceEqual/Shuffle/SkipLast/SkipWhile/TakeLast/TakeWhile/UnionBy/Zip` 没有 EF Core 10 关系型翻译入口；不采用隐式客户端回退。
9. **数据库限制**：`AlterDatabaseOperation` 在线修改现有数据库 collation 不受支持。
10. **环境拓扑限制**：单节点不能验证真实主备切换、网络分区和拓扑健康检查。
11. **尚需扩大验证**：官方 relational specification tests、Oracle/pg 双模式 CI、Database First 高级对象和大规模压力测试。

## 17. 兼容度结论

| 维度 | 当前评估 |
|---|---|
| DbContext/DI/连接 | 92% - DataSource、单节点 MultiHost 和小池并发已验证；真实故障转移拓扑不足 |
| 基础 CRUD | 90% - 图和 Range、批处理、默认/计算值、并发异常、级联均已验证；存储过程保存未实现 |
| 基础 LINQ | 90% - 常用运算符及 JSON/数组/range/全文基础函数已验证；窗口和 EF 无入口项除外 |
| EF Core 10 新能力 | 72% - complex、DayNumber、集合参数和 NativeAOT 已验证；JSON 聚合模型仍缺 |
| 模型映射 | 85% - 通用关系型模型、关系、继承、拆分、视图、生成值均已验证；高级类型和存储过程映射除外 |
| Migration | 90% - 历史表、锁、升级/回滚、幂等脚本和除 AlterDatabase collation 外的公开操作已实现/验证 |
| Database First | 80% - CLI scaffold、表/视图/键/索引/FK/序列和配置代码生成已验证；高级对象仍需扩展 |
| 高级数据库类型 | 55% - jsonb 标量、integer[]、int4range、tsvector/tsquery 已实现 |
| 生产可用性 | **核心 CRUD/查询可试生产；“EF Core 10 全语法生产可用”仍不满足** |

这些百分比是基于功能项覆盖的工程估算，不是官方认证数字。

## 18. 要达到“所有公共关系型语法支持”的验收条件

1. 接入 EF Core 10.0.9 `EFCore.Relational.Specification.Tests` 全部适用测试族。
2. 每个跳过测试必须记录明确的 Kingbase 数据库限制，不能以“未实现”长期跳过。
3. 建立 Oracle 模式和 pg 模式两套真实库 CI。
4. 完成剩余 Kingbase 方法/成员/aggregate translator provider，并持续维护 63/39/6 API 自动契约。
5. 扩展现有 JSONB/int[]/range/全文基础映射到 JSON 聚合、更多数组类型、空间和 NodaTime。
6. 完成存储过程保存、JSON `ExecuteUpdate`，并扩展批次边界和压力测试。
7. 扩展 Migration 的跨 schema、失败恢复和超大脚本压力测试，并记录 AlterDatabase 的数据库限制。
8. 扩展 Database First 的 trigger、表达式索引、分区表和高级对象反向工程。
9. 完成连接池、取消、故障转移和网络故障压力测试。
10. 发布 API 支持矩阵，所有未支持语法在查询编译期给出明确异常。

## 19. 最终确认

**当前版本不能对外宣称“EF Core 10 所有语法均支持”。**

可以对外声明的范围是：

> 在 KingbaseES V009R001C002B0014 Oracle 兼容模式、Kdbndp_V9 10.0.1.703 和 EF Core 10.0.9 组合下，Provider 已通过 58 项 Release 回归，其中 34 项为真实 Kdbndp 集成测试。本轮新增并验证 complex、参数集合、DayNumber、微秒/纳秒、null compensation、Enum、byte[]、JSON/数组/range/全文基础类型和函数、Index INCLUDE、Seeding、通知跟踪、DataSource/MultiHost、连接池、Interceptor、连接串替换，以及 NativeAOT 预编译、发布和真实运行。仍不能宣称全语法支持：JSON complex/owned、HiLo、统一 rowversion、存储过程保存、窗口 API、Spatial/NodaTime、真实主备故障转移和完整 relational specification suite 尚未完成。

## 20. 官方资料

- EF Core 10 What's New: https://learn.microsoft.com/ef/core/what-is-new/ef-core-10.0/whatsnew
- EF Core 10 Breaking Changes: https://learn.microsoft.com/ef/core/what-is-new/ef-core-10.0/breaking-changes
- EF Core Querying: https://learn.microsoft.com/ef/core/querying/
- EF Core Modeling: https://learn.microsoft.com/ef/core/modeling/
- EF Core Saving Data: https://learn.microsoft.com/ef/core/saving/
- EF Core Migrations: https://learn.microsoft.com/ef/core/managing-schemas/migrations/
- EF Core Provider Authoring: https://learn.microsoft.com/ef/core/providers/writing-a-provider
- EF Core source tag v10.0.9: https://github.com/dotnet/efcore/tree/v10.0.9
